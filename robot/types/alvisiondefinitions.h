// these are the same values as alvision/alvisiondefinitions.h so we don't need a CTC
// really we could make our own constants, but I'm too lazy to change NaoCamera et al
enum AL {
   kQVGA    = 1,  // 320*240
   kVGA     = 2,  // 640*480
   k960p    = 3,  //1280*960
};
